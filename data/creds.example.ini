; Copy this file to creds.ini and fill in your own values.
; creds.ini is gitignored and must never be committed.

; Optional: with an OpenAI key the admin auto-translates new products and
; categories to French, Spanish and German when you save them. Without a key
; the English master text is copied and you edit translations in the admin.
;OpenAI = sk-...

; Protects the admin (the demo.admin.* hostname / port 8081) with HTTP basic
; auth. Without a creds.ini the admin is open, which is fine for local play.
[dashboard]
user     = admin
password = change-me
